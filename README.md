# MoySklad Order Maker

Веб-приложение для создания заказов в системе МойСклад через API. Позволяет просматривать доступные товары, создавать заказы и просматривать историю заказов.

## 🚀 Возможности

- ✅ **Аутентификация пользователей** - вход по email с определением уровня цен
- ✅ **Просмотр товаров** - отображение доступных товаров с ценами в зависимости от уровня пользователя
- ✅ **История заказов** - просмотр истории заказов с сохранением в localStorage
- ✅ **Детали заказа** - просмотр детальной информации о конкретном заказе
- ✅ **Уведомления по email** - отправка уведомлений о новых заказах через EmailJS
- ✅ **Сохранение сессии** - автоматическое восстановление сессии пользователя при перезагрузке страницы
- ✅ **Отзывчивый дизайн** - адаптивный интерфейс для различных устройств

## 🛠 Технологии

- **React 19** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **React Router DOM 7** - маршрутизация
- **Webpack 5** - сборка и бандлинг
- **Jest** - тестирование
- **ESLint + Prettier** - линтинг и форматирование
- **EmailJS** - отправка email уведомлений
- **MoySklad API** - интеграция с системой МойСклад

## 📋 Требования

- Node.js 20 или выше
- npm или yarn
- Аккаунт в МойСклад с API доступом
- Аккаунт в EmailJS (для уведомлений)

## 🔧 Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd moyorder
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env  # если есть пример
# или создайте файл вручную
```

4. Настройте переменные окружения (см. раздел [Конфигурация](#конфигурация) ниже)

## Конфигурация

Создайте файл `.env` в корне проекта со следующими переменными:

### Обязательные переменные

```env
# Учетные данные МойСклад
MOYSKLAD_USERNAME=your_username          # Имя пользователя МойСклад (опционально)
MOYSKLAD_PASSWORD=your_password          # Пароль или токен API МойСклад (обязательно)

# Email для уведомлений о заказах
ORDER_NOTIFICATION_EMAIL=notifications@example.com
```

### Переменные для EmailJS (для уведомлений)

```env
EMAILJS_PUBLIC_KEY=your_public_key
EMAILJS_SERVICE_ID=your_service_id
EMAILJS_TEMPLATE_ID=your_template_id
```

## 🏃 Запуск

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:9000`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

## 🗺 Роуты

Приложение использует React Router для навигации:

- `/` - Страница входа (LoginPage)
- `/stock` - Просмотр доступных товаров и создание заказов (StockPage)
- `/history` - История заказов (OrderHistoryPage)
- `/history/:orderId` - Детали конкретного заказа (OrderDetailPage)
- `/about` - О приложении (AboutPage)

## 👤 Аутентификация

Приложение использует простую систему аутентификации на основе email:

- Разрешенные email адреса настроены в `src/models/auth-model.ts`
- Сессия пользователя сохраняется в `localStorage` и автоматически восстанавливается при перезагрузке страницы
- Каждому email соответствует уровень цен (basic или level1)

### Настройка разрешенных пользователей

Отредактируйте файл `src/models/auth-model.ts`:

```typescript
const PRICE_LEVEL_BY_EMAIL: Record<string, PriceLevel> = {
  "small@beer.ru": "basic",
  "big@beer.ru": "level1",
};
```

## 🔒 Безопасность

⚠️ **Важно:**

- Никогда не коммитить файл `.env` в репозиторий
- Учетные данные МойСклад хранятся только в переменных окружения
- В production использовать безопасные методы хранения секретов
- **Внимание**: При деплое на GitHub Pages секреты встраиваются в клиентский код и становятся видимыми в браузере. Это означает, что любой пользователь может увидеть ваши учетные данные МойСклад в исходном коде страницы. Для production рекомендуется использовать прокси-сервер или backend API для защиты секретов.

## 📝 Лицензия

ISC
